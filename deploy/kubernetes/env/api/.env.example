# Example environment configuration for MonitorMe API in Kubernetes
# Copy this file to .env and update with your actual values

DB_HOST=postgres-service
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_secure_password
DB_NAME=monitorme
